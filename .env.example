# Frontend build-time config only.
# Values prefixed with VITE_ are bundled into the React app and visible to users.

VITE_APP_NAME="SHOKO by Loka15"

# Leave unset for packaged Tauri builds. Tauri starts the backend on a free
# localhost port and passes the resolved URL to React at runtime.
# VITE_API_BASE_URL="http://127.0.0.1:8000"
